server:
  servlet:
    context-path: /loja-da-joyce
    
spring:  datasource:    username: docker
    #password: docker
    password: ${MYSQL_PASSWORD}
    #url: jdbc:mysql://127.0.0.1:3306/loja_db?useTimezone=true&serverTimezone=UTC&useSSL=false
    url: jdbc:mysql://db:3306/${MYSQL_DATABASE}?useTimezone=true&serverTimezone=UTC&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver  jpa:    hibernate:      ddl-auto: none    show-sql: false  flyway:    #url: jdbc:mysql://127.0.0.1:3306/
    url: jdbc:mysql://db:3306/
    schemas: loja_db
    user: docker
    password: docker
     security-data:
  secret_key: secret
  expire_lenght: 3600000 #equivalente a 1 hora